import { useState } from 'react';
import { useRecoilState, useRecoilValue } from 'recoil';

const Sam = (): JSX.Element => {
  return (
    <div>
      <div></div>
    </div>
  );
};

export default Sam;
